%h3= "#{@institute.name}"

%table.table.table-striped
    %tbody
        %tr
            %th Name 
            %th

        %tr
            %td= @institute.name
            %td= link_to "Edit", edit_institute_path(@institute)

= link_to "Back", institutes_path, class: 'btn btn-info'
